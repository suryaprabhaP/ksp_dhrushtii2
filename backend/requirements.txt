# ==============================================================================
# KSP Sentinel AI — Cloud-Native Lightweight Backend (AppSail Optimized)
# All heavy ML/data libs REMOVED — replaced by Zoho Catalyst cloud services:
#   pandas/numpy/scikit-learn  → Catalyst QuickML + ZCQL DataStore
#   duckdb                     → Catalyst ZCQL (SQL over REST, already wired)
#   shapely                    → Pure Python Convex Hull Engine (Zero C-extensions)
# Container size target: ~25MB (down from ~400MB)
# ==============================================================================

# Core Web Framework, CORS & WSGI Server
Flask==3.1.3
flask-cors==6.0.2
Werkzeug==3.1.3
gunicorn==21.2.0

# High-Speed LLM Inference Providers (Cloud API calls only, zero local models)
groq==0.37.1
google-generativeai==0.8.6
google-genai==2.14.0

# HTTP Networking & Zoho Catalyst REST Integrations
requests==2.32.3
urllib3>=2.0.0

# Environment Configuration Loader
python-dotenv==1.2.1

# Pure Python Document & Dataset Parsers (Zero C-Extension Compilation)
openpyxl>=3.1.0
pypdf>=3.17.0

# Pure Python Live Database Connectors (Zero C-Extension Compilation)
pg8000>=1.31.0
PyMySQL>=1.2.0
pymongo>=4.6.0
